# 0.2.2 · 音视频使用宿主工具

- 移除内置音视频转写和视频抽帧，由当前宿主实际可用的工具处理；不再要求 FFmpeg、macOS 26 或系统语音资源。
- `source_import` 接收宿主工具的真实结果、定位及覆盖说明，保存原件与来源记录；部分结果保留缺口，不计为完整读取。
- 旧 ready 媒体归档继续可读；未完成的旧媒体任务不自动恢复本地处理，由宿主重新处理并重新导入，保留历史。
- 访谈、本地保存、流程恢复与只读预览继续保留。当前仍以 macOS 为支持范围，需要 Node.js 22.13+；扫描件 OCR 仍使用 Apple Vision 与 Xcode Command Line Tools。
- 本次未追加自动测试，新的宿主音视频处理路径尚未完成实际体验验收。

# 0.2.1 · 直接开始创作

无需提供 buddyid；新创作自动生成内部标识及独立目录。宿主保存启动身份以便重试，继续当前项目使用已保存目录，避免误载默认旧草稿。

# 0.2.0 · 完整 Skill

- 将采访规则、工作流、资料工具与只读预览打包成 buddy-creator skill，随包提供生产依赖。
- 完成后自动生成本地四册、汇总手册、服务图和来源版本记录，失败可恢复。
- 移除模拟开发交接与小红书账号采集；保留历史资料与确认记录。
- 预览升级保留固定地址；修正 macOS 临时路径别名的目录判断。
- 本包在 macOS Apple Silicon 构建。独立包和自动流程检查不代替三个宿主实际体验验收。

# Buddy Assistant 0.1.17 · macOS 试用候选版

四册确认后自动交接到模拟 coding CLI，默认流程从下载资料包改为展示开发接收回执。

## 本次更新

- 最后一次正式确认通过门槛后，自动准备本版本的内部资料包，调用独立的本地 mock coding CLI 并保存接收回执。
- 主对话与半屏预览呈现“模拟交接，等待开发”，两个内容视图都可查看；不自动开启新一轮采访或要求再次确认交接。
- 同一版本只接收一次；重启、重试及接收结果恢复复用同一交接身份。用户新修订先处理，新版本确认前不以旧回执代表已交接。
- 内部资料包固定版本与摘要；原按需导出接口仍可用。旧项目重新打开会补做自动交接，保留原内容与预览地址。

## 试用范围

当前接收方是 mock，只校验和接收资料，没有生成代码、启动真实开发、收费或部署。原始资料缺口和上线准备事项继续保留在方案中。完整三宿主体验验收范围保持不变。

6 项交接检查通过，覆盖门槛、暂停、跨宿主待处理修订、并发幂等、资料损坏恢复和接收回执恢复；一条宿主主对话智能访谈全流程验证了最后确认自动交接及新修订保护。CLI 与预览构建通过，未重跑完整三宿主体验验收。
